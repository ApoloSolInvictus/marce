# Client Painting Image Map

This demo uses generated placeholder paintings only. Replace the files below with the client's real paintings when they are ready.

## Main Gallery Slots

1. `images/paintings/client-slot-01-crimson-eye.jpg`
   - Used in `index.html` and `gallery.html`
   - Slot note in HTML: `CLIENT IMAGE SLOT 01`
   - Suggested real artwork: first featured painting or hero vertical painting

2. `images/paintings/client-slot-02-geometric-eyes.jpg`
   - Used in `index.html`, `gallery.html`, and the homepage hero background in `style.css`
   - Slot note in HTML: `CLIENT IMAGE SLOT 02`
   - Suggested real artwork: wide or horizontal painting

3. `images/paintings/client-slot-03-luminous-eye.jpg`
   - Used in `index.html` and `gallery.html`
   - Slot note in HTML: `CLIENT IMAGE SLOT 03`
   - Suggested real artwork: colorful square painting

4. `images/paintings/client-slot-04-charcoal-eye.jpg`
   - Used in `index.html` and `gallery.html`
   - Slot note in HTML: `CLIENT IMAGE SLOT 04`
   - Suggested real artwork: high-contrast or monochrome painting

## Extra Placement Slots

Slots 05 through 09 reuse the same demo images so the layout is full. When the client provides more real works, replace the repeated file references in `index.html` and `gallery.html` near comments `CLIENT IMAGE SLOT 05` through `CLIENT IMAGE SLOT 09`.

Keep replacement images web-optimized when possible:

- Use `.jpg` for paintings and photos.
- Keep the same filenames if you want the HTML to work without edits.
- Aim for files under 800 KB each for fast loading.
